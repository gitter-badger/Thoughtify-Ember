import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:user-comments', 'UserCommentsView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
